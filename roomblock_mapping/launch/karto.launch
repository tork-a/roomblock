<launch>
  <arg name="use_bag" value="true"/>

  <!-- Specify the bag file -->
  <arg name="bag_file" default="$(find roomblock_mapping)/bags/2017-03-15-10-49-47.bag"/>

  <!-- Use simulated timestamp of the bag file -->
  <param name="use_sim_time" value="true"/>

  <!-- Load robot description -->
  <param name="robot_description" command="$(find xacro)/xacro.py '$(find roomblock_description)/robots/roomblock.urdf.xacro'" />

  <!-- Publish tf from joint_state -->
  <node pkg="robot_state_publisher" type="state_publisher" name="robot_state_publisher"/>

  <!-- Play rosbag -->
  <node pkg="rosbag" type="play" name="player" output="screen" args="--clock $(arg bag_file)"/>

  <!-- Start slam_karto -->
  <node name="slam_karto" pkg="slam_karto" type="slam_karto" output="screen" />

  <!-- Start rviz -->
  <node name="rviz" pkg="rviz" type="rviz" args="-d $(find roomblock_mapping)/launch/mapping.rviz"/>

</launch>
